using UnityEngine;

namespace CPS.ICPBL.Common
{
    /// <summary>
    /// 디버그 시각화 헬퍼 (preliminary stub).
    /// 운영 위치 12개 Gizmo, 로봇 경로 라인, 자원 락 상태 표시 등을 모은다.
    /// 사용처(2-4c OperatingStations, 2-5 ICPBLScorer)가 명확해질 때 구체화.
    /// </summary>
    public static class DebugVisualizer
    {
        // 추후 구현 예정.
    }
}
